import React from 'react'
/** @jsx jsx */
import { css, jsx } from '@emotion/core'

const Header = () => (
  <React.Fragment>
    <div>User</div>
    <div>Sign Out</div>
  </React.Fragment>
);
 export default Header